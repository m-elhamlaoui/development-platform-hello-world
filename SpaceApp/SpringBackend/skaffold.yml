apiVersion: skaffold/v4beta6
kind: Config
metadata:
  name: microservices-app
build:
  local:
    push: false
  artifacts:
    - image: collision-service
      context: collision-service
      docker:
        dockerfile: Dockerfile
    - image: my-microservice-b
      context: gateway-service
      docker:
        dockerfile: Dockerfile
deploy:
  kubectl:
    manifests:
      - k8s/deployment.yaml
